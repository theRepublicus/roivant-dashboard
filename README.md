# roivant-dashboard

Interactive Roivant Sciences company file: Vant history, company structure, key
transactions and a scenario lab. One static page (`index.html`), no build step.

Served by GitHub Pages from the root of `main`; every push redeploys.

Scenario-lab unit values and catalyst probabilities are illustrative defaults, not estimates.
